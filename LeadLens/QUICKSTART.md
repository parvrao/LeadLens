# Prospera — Quick Start Scripts

## Start Backend
```bash
cd backend && npm install && cp .env.example .env && node server.js
```

## Start Frontend
```bash
cd frontend && npm install && npm run dev
```

## One-line dev startup (run both)
```bash
# Terminal 1
cd backend && npm run dev

# Terminal 2
cd frontend && npm run dev
```
